#include "main.h"
/**
 * main - prints the aplhabet in lowercase
 *
 * Return: 0
 */
int main (void)
{
	print_alphabet()
	{
		char a;
		for (a = 'a' ; a <= 'z' ; a++)
			putchar(a);
		putchar('\n');
	}
	return (0);
		
}
