#include "main.h"
/**
 * print_array - prints out an array of number n
 * @a: the first int
 * @n: the second int
 * Return: o
 */

void print_array(int *a, int n)
{
	int a;

	for (x = 0; x < n; x++)
		if (x != n - 1)
			printf("%d", a[x]);
		else
			printf("%d", a[x]);
	printf("\n");
}
