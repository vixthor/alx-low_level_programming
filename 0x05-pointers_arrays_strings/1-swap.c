#include "main.h"
/**
 * swap_int - swaps values between two integers
 * @a: the value
 * @b: the second value
 * Return: 0
 */

void swap_int(int *a, int *b)
{
	int c;

	c = *a;
	*a = *b;
	*b = s;
}
